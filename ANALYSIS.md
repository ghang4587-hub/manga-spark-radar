# 原交付分析与“漫剧抓取”改造说明

分析日期：2026-08-28

## 结论

原交付是一个静态单页样本看板，不是持续运行的抓取系统。它可以浏览 30 条预置 YouTube 视频、筛选标签、打开页内播放器，并利用 YouTube storyboard 地址补充画面，但没有发现新视频、定时运行、更新数据或自动部署的完整链路。

因此，原页面**不会真正每周更新**。页面里的“样本库每周自动刷新”“本周新增”属于前端展示文案与日期判断，并不能证明后台存在自动任务。当前漫剧版已补上可执行的周更脚本和 GitHub Actions；只有将本目录的 `.github/workflows/weekly-update.yml` 部署到目标仓库后，周更链路才会生效。

### 线上版本核验

用户提供的线上地址 `https://ghang4587-hub.github.io/hanbox/?v=2e9f579` 也不具备自动周更能力：

- 对应公开仓库 `ghang4587-hub/hanbox` 的根目录只有 `index.html`。
- GitHub Actions 显示的 19 次运行全部是 `pages-build-deployment`；它们在代码被提交后负责发布 Pages，不负责采集数据。
- 仓库 19 次提交集中在 2026-08-26 至 2026-08-27，属于人工连续迭代，没有跨周定时运行证据。
- 线上脚本仍内嵌 30 个固定视频 ID，并存在 4 个同名 `render()` 定义。
- 链接参数 `?v=2e9f579` 未被页面通过 `location.search` 或 `URLSearchParams` 读取。它只是缓存标记，不会锁定该提交，也不会触发抓取。

准确表述应是：**人工提交 `index.html` 后，GitHub Pages 会自动重新发布；样本数据本身不会每周自动采集。**

## 原交付结构

| 位置 | 作用 | 判断 |
| --- | --- | --- |
| `site-repo/index.html` | GitHub Pages 源码 | 56 KB 左右的单文件网页，数据与逻辑全部内嵌 |
| `deploy/index.html` | 部署副本 | 与 `site-repo/index.html` 完全相同 |
| `outputs/merge-spark-radar-video.html` | 本地浏览副本 | 与上述两份完全相同 |
| `outputs/merge-spark-radar.html` | 早期版本 | 历史静态版 |
| `work/storyboards.json` | 30 个既有视频的 storyboard 参数 | 只服务于分镜图展示 |
| `work/fetch_storyboards.py` | 读取 HTML 中已有视频 ID，再抓其 storyboard | 不负责搜索或发现新视频 |
| `work/apply_storyboard_extras.py` | 把 storyboard 数据补写到三份 HTML | 属于构建补丁，不是定时采集 |

## 原交付为什么不会每周更新

缺少以下任一可执行的周更链路：

1. 没有 GitHub Actions、Windows 计划任务、cron 或云函数配置。
2. 没有 YouTube Data API 密钥或替代的数据发现接口。
3. 30 条视频对象直接写在 `index.html` 中，新视频不会自动进入数组。
4. `fetch_storyboards.py` 只从现有 HTML 提取 ID；它无法发现频道的新上传。
5. 没有“采集 → 清洗/打标 → 写回页面 → 提交代码 → 发布”的自动流程。
6. 固定的周起始日期和“每周自动刷新”文案只会改变视觉标记，不会改变样本库。

## 现有实现的产品与工程问题

- 主题混杂：README 称“短剧买量创意”，用户描述又指“海外影视热点”，页面数据实际聚焦 DramaBox / GoodShort 长视频样本；需要先确定唯一产品口径。
- 数据与观点未分层：播放量、发布时间等平台事实，与 Hook 分、剧情梗概、买量提示等编辑判断混在同一数据对象中，用户容易误认为都是抓取结果。
- 更新状态不诚实：没有定时任务，却显示“每周自动刷新”。
- 代码是连续补丁式演进：同一个 `render()` 在页面里出现多次，后定义覆盖前定义；多段 CSS 和事件绑定重复，维护成本较高。
- 导航存在空功能：侧栏“冲突拆解、素材收藏、抓取设置”等按钮只是视觉元素，没有对应页面或行为。
- 数据源脆弱：播放器、封面和 storyboard 都依赖 YouTube 外链；受地区、嵌入许可、跨域策略和视频下架影响。
- 分镜参数会过期：storyboard 地址包含签名，长期静态保存并不可靠。
- 缺少可追溯性：没有逐条记录采集时间、来源页面、清洗规则和分析生成方式。

## 本次漫剧改造

新页面没有简单替换品牌文案，而是更换了真实样本与分析口径：

- 抓取主题改为英文配音动态漫 / 漫剧长视频。
- 采集两个公开 YouTube 频道的 30 条最新样本：Baichuan Comics 15 条、Supreme Anime Drama 15 条。
- 记录视频 ID、英文原标题、频道、时长、采集时显示的播放量与相对发布时间。
- 新增中文编辑标题、题材标签、标题钩子、升级路径和留存承诺。
- 筛选项改为来源、题材、时长和排序；题材围绕系统、末日、修仙、重生与异兽。
- 初始版本曾明确标注“静态数据快照”；本次已增加可执行周更链路，部署工作流后由 Actions 接管快照更新。
- 重写为单一渲染流程，避免原页面多次覆盖 `render()` 的问题。

### 与原站再次对比后补齐（2026-08-28）

- 为 30 条漫剧重新抓取 YouTube 公开 storyboard，并通过 Android 播放器接口读取每条视频的英文字幕。
- 对字幕上下文按生死危机、正面对抗、能力觉醒、身份揭示、受辱背叛、反击决断、关系转折等语义评分，使用宽区间覆盖 + 全局高分去重，选出 10 个非均匀剧情 / 冲突节点；节点时间不再按 0%–100% 固定百分比生成。
- 时间轴节点下方显示实际字幕上下文，支持点击跳转播放器对应秒数，并保留 storyboard 加载失败时的封面降级；字幕不可用时明确标记“视觉候选”，不伪造剧情描述。
- 补齐 4 步用户心流：注意力、情绪、信息差、留存问题。
- 补齐按题材生成的漫剧买量改编提示。
- 卡片补齐结构梗概、近 7 日新样本标记和受众结构标签。
- 工具栏补齐受众结构筛选；当前快照分布为男频向 26 条、女频向 1 条、中性待判 3 条。

没有照搬原站的空侧栏导航；周更状态现在对应实际的 Actions 配置，而不是单纯的前端文案。

## 对样本的内容判断

本批 30 条样本表现出非常集中的标题公式：

> 低位身份 / 世界灾难 + 可一句话解释的外挂机制 + 量级极大的终局承诺

最常见的能力包装是“系统”，其次是末日、修仙、异兽进化、重生 / 穿越。标题普遍把完整爽点直接写出，而不是依靠含蓄悬念：例如“废物被嘲 → 系统觉醒 → 弑神”“极寒末日 → 御兽觉醒 → 建立生存秩序”。这说明海外英文动态漫的标题竞争重点是快速可译、低理解成本与高量级结果承诺。

需要注意：页面中的 Hook 估分和中文拆解是编辑判断，不是 YouTube 官方指标，也没有逐条观看完整视频或核验完整剧情。

## 周更实现与运行边界

推荐最小可行架构：

```text
GitHub Actions（每周定时）
  → YouTube Data API / 稳定采集器（发现频道新视频）
  → 数据清洗与去重（video_id 为主键）
  → 规则或模型打标（题材、Hook、中文摘要）
  → 生成 snapshot.json
  → 自动提交仓库
  → GitHub Pages 发布
```

还应补充：失败告警、API 配额监控、数据更新时间、来源 URL、删除/下架处理、人工抽检，以及“平台事实”和“编辑分析”的字段分离。

漫剧版现在提供了最小可行实现：`scripts/weekly_update.py` + `.github/workflows/weekly-update.yml`。它每周一 08:00（北京时间）读取频道 RSS，检查视频页的可播放状态，按本周新发布和播放速度选出固定 30 条，并自动提交触发 Pages 发布。若工作流尚未部署，仍应把产品状态视为手动快照。

脚本的筛选边界：明确返回非 `OK` 播放状态的视频会被剔除；网络超时等临时错误会保留上一周记录。播放量阈值默认 200，候选不足 30 条时才会放宽，以保证页面始终维持固定样本量。

## 数据边界

- 样本来自 2026-08-28 对公开 YouTube 频道页面的浏览器核验。
- 播放量是采集时快照，会自然变化。
- 相对发布时间被换算为排序用日期时只作近似值；页面仍展示原相对时间。
- YouTube 可能限制嵌入播放；发生时可使用页面里的“YouTube 原片”链接。
- 本次交付不包含视频文件，也不重新分发第三方媒体。
